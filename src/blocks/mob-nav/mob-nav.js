// document.addEventListener('DOMContentLoaded', function(){});
(function(){
  /*let burgerBtn = document.getElementById('burger');
  let body = document.querySelector('body');
  //console.log(burgerBtn);
  const burgerClickHandler = function (e) {
    //console.log(e.target.ariaExpanded);
    body.style = 'overflow:hidden;';
    if (e.target.ariaExpanded !== false) {
      console.log(e.target.ariaExpanded);
    }
  };*/

  // const setTimeOut = function (e) {
  //   window.setTimeout(burgerClickHandler(e.target), 3000);
  // };

  //burgerBtn.addEventListener('click', burgerClickHandler);
}());
